services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: iff
      POSTGRES_PASSWORD: iff
      POSTGRES_DB: iff
    volumes:
      - ../data/postgres:/var/lib/postgresql/data